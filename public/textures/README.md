Coloca aqui la textura real de papel:

- paper-texture.jpg

Si tu archivo esta en PNG, usa:

- paper-texture.png

La invitacion usa primero `/textures/paper-texture.jpg` y tambien deja preparada la ruta `/textures/paper-texture.png`.
